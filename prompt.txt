Refer CLAUDE.md and Plan.md. 

In backend/routers/contracts.py, add/confirm:

GET /api/contracts/{id}
- Depends on get_current_user_id
- Fetch the contract doc filtered by _id AND freelancer_id (404 if not found
  or not owned by this user)
- Fetch its milestones (db.milestones.find({"contract_id": id, "freelancer_id": freelancer_id}))
- Return {"contract": {...}, "milestones": [...]}

In backend/routers/milestones.py, add:

GET /api/milestones/{contract_id}
- Depends on get_current_user_id
- Return db.milestones.find({"contract_id": contract_id, "freelancer_id": freelancer_id})
  as a list, 404 if the contract itself doesn't belong to this freelancer

Both endpoints must filter by freelancer_id — no exceptions, per the
handoff's data-model rule. Don't wire the Phase 4 lazy-check BackgroundTask
into these yet — that's Phase 4's job, not this step's.

log updates CLAUDE.md and Plan.md with the exisiting format