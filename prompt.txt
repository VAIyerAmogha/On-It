Refer CLAUDE.md and Plan.md. 

Modify the Invoice Preview page component (locate the existing invoice
detail/preview page from Phase 7 — do not create a duplicate page).

1. Add a "Send Invoice to Client" button, styled per the locked design
   system (glass-surface, Violet + Graphite accent, Lucide icon — use
   something like `Send` or `Mail`).

2. On click, call GET /api/invoices/{id}/email-preview and open a modal
   (glass-surface dialog, matching existing modal patterns already used
   elsewhere in the app — e.g. the upload modal) showing:
   - A read-only "To" field (client email) and "From" field (app Gmail
     address) — these are not editable.
   - An editable Subject input, pre-filled from the preview response.
   - An editable Body textarea, pre-filled from the preview response.
   - Two actions: "Cancel" (closes modal, discards edits) and
     "Send" (confirms).

3. On "Send", call POST /api/invoices/{id}/send with the current
   (possibly edited) subject/body from the modal's local state — do not
   re-fetch the preview on submit, send exactly what's in the form.

4. Handle loading/success/error states calmly per the design system (no
   alarming red error banners — muted, consistent with how low-faithfulness
   QA states are styled). On success: close the modal, show a subtle
   success state, and update the invoice's displayed sent_at / "sent"
   status on the page without requiring a full page reload.

5. If invoice.sent_at is already set when the page loads, change the
   button label to "Resend Invoice to Client" (same flow, same modal) and
   show the existing sent_at timestamp near the button so the user knows
   it was already sent once.

6. Do not add any new global state management — follow whatever local
   state / existing data-fetching pattern (AuthContext-style fetch wrapper
   via api.ts) the rest of the invoice page already uses.


log updates CLAUDE.md and Plan.md with the exisiting format