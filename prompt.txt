Refer CLAUDE.md and Plan.md. 

In backend/routers/contract_qa.py, add:

POST /api/contracts/{id}/ask

- Depends on get_current_user_id, ownership check on the contract (404 if
  not found/not owned)
- 400 if contract["indexed_for_rag"] != True (with a message like
  "This contract is still being indexed — try again shortly")
- Body: {"question": string}
- Call lib.rag.ask_contract(db, id, freelancer_id, question)
- Return the result dict as-is (answer, cited_sections, faithfulness_score)

log updates CLAUDE.md and Plan.md with the exisiting format