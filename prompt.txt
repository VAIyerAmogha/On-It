Refer CLAUDE.md and Plan.md. 

The invoice page and PDF export still aren't showing up. Don't ask me for
milestone IDs, invoice IDs, or auth tokens — you have full access to the
codebase and the terminal, so get everything you need yourself:


Register your own throwaway test user (or reuse one already in the DB),
upload the sample fixture already in backend/tests/fixtures/, generate
a milestone/invoice through the actual API, and use the IDs/token that
produces. Or just read the source files directly — you don't need to
run anything to catch a field-name mismatch or a missing/misnamed file.


Do this, in order


Read the actual files, don't assume. Open every file in this chain
and check them against each other:

The backend invoice-creation endpoint (routers/milestones.py) —
what shape does it actually return? Is _id serialized as a plain
string, or is it leaking a raw ObjectId?
Whatever frontend code handles the response after generating an
invoice — does it read .id, ._id, or .invoice_id? Does that
match what the backend actually sends? This exact mismatch is the
single most likely bug — check it first.
Does pages/invoices/[id].tsx exist, at exactly that path? Next.js
Pages Router is filename-sensitive; a misnamed file 404s silently.
Does a "Generate Invoice" button/handler actually exist in the code
and actually call the endpoint and actually navigate afterward — or
is one of those three missing/dead code?
Does components/InvoicePreview.tsx exist and get imported into the
invoices page, with the pdfjs worker actually configured?
Does a Download button exist anywhere in the frontend code at all —
search for it. If it was never added, that's simply missing — build
it, don't assume it's "broken."



Fix whatever you find, using your own runtime test (register →
upload → generate invoice → visit the page → download) to confirm the
fix actually works, end to end, before considering it done.
Download button spec (build this if it doesn't exist): a visible
"Download PDF" button that fetches GET /api/invoices/{id}/pdf with
the Authorization header attached, reads it as a blob, and triggers a
real file download via an object URL — a bare <a href> won't carry
auth and will fail.


Report back concretely what was actually broken and what you changed —
not just "fixed it."

log updates CLAUDE.md and Plan.md with the exisiting format