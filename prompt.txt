Refer CLAUDE.md and Plan.md. 

Build pages/settings.tsx:

- GET /api/settings on mount, populate a form (glass surface, sectioned
  into logical groups: Profile, Bank Details, Invoicing, Email Templates —
  each section its own subtle glass card rather than one giant form)
- Fields: name, address, gstin, bank_name, account_number, ifsc, upi_id,
  gmail_address, default_gst_rate, invoice_prefix
- On submit, PUT /api/settings with the full updated payload, show a calm
  success confirmation (subtle toast or inline check + fade, not a jarring
  banner)
- Keep gmail_app_password write-only (a password-type input that's never
  pre-filled with the existing stored value, and only sent to the backend
  if the user actually typed a new one — don't round-trip the stored
  secret back into the form)

log updates CLAUDE.md and Plan.md with the exisiting format