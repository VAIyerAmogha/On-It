Refer CLAUDE.md and Plan.md. 

Modify the existing register page, add a new verify-email page, and modify
the existing login page. Follow the locked design system (glass-surface,
Violet + Graphite, Lucide icons, calm non-alarming states).

1. Register page: on successful POST /api/auth/register (manual path),
   since it no longer returns a JWT, do not redirect to the dashboard.
   Instead show a calm "check your inbox" glass-surface state on the same
   page (email icon, message telling them to click the link sent to
   {email}). Include a "Resend verification email" link/button that calls
   POST /api/auth/resend-verification with the registered email and shows
   a brief confirmation toast/state (not an alarming error style even if
   it silently no-ops server-side).

2. New page pages/verify-email.tsx (Pages Router, matches existing routing
   convention):
   - Reads `token` from the query string.
   - On mount, calls GET /api/auth/verify-email?token=... .
   - Success: show a calm success state with a "Go to Login" button/link
     to /auth/login.
   - Failure (invalid or expired token): show a calm (not alarming) error
     state explaining the link is invalid/expired, with a way to enter
     their email and trigger resend-verification from this same page
     (reuse the same resend call as the register page — don't duplicate
     the fetch logic, share a small helper in api.ts).

3. Login page: when POST /api/auth/login returns 403 with
   error_code "EMAIL_NOT_VERIFIED", do not show the generic
   wrong-credentials error state. Show a distinct message ("please verify
   your email first") with a "Resend verification email" action, reusing
   the same shared resend helper from api.ts. Google OAuth sign-in on this
   same page is unaffected — only the manual-login error branch changes.

4. Do not add any new global state management — follow the existing
   AuthContext / api.ts fetch wrapper pattern already used on these pages.


log updates CLAUDE.md and Plan.md with the exisiting format