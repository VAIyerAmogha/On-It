Refer CLAUDE.md and Plan.md. 

Modify the Dashboard notification panel built in the dashboard notification
system feature. Add rendering for the three new notification types
alongside the existing four. Follow the locked design system throughout.

1. MISSED_DELIVERY row:
   - Left: Lucide `XCircle` icon in muted red (this IS the most urgent
     state — slightly stronger visual than amber, still not harsh).
     "{contract_title} — Milestone {milestone_number}" bold,
     "{client_name}" muted below.
   - Center: "Delivery missed by {days_overdue} days — not yet triggered"
     with trigger_date shown as human-readable date in muted text below.
   - Right: "Mark as Triggered" button (calls existing
     PATCH /api/milestones/{id}/trigger, removes row on success) and
     "Missed Deadline Invoice" button (navigates to the contract detail
     page with the milestone pre-scrolled into view, where the user can
     use the Missed Deadline flow from Feature 1 — do not inline the
     discount modal here, just navigate). And "View" button.

2. UPCOMING_DEADLINE row:
   - Left: Lucide `CalendarClock` icon in muted violet.
     "{contract_title} — Milestone {milestone_number}" bold,
     "{client_name}" muted.
   - Center: "Due in {days_until_deadline} days —
     {deliverable_description truncated to 60 chars}" with trigger_date
     as human-readable date in muted text.
   - Right: "View" button only.

3. UNINVOICED_TRIGGERED row:
   - Left: Lucide `FileWarning` icon in muted amber.
     "{contract_title} — Milestone {milestone_number}" bold,
     "{client_name}" muted.
   - Center: "Triggered — deadline passed {days_since_triggered} days ago,
     invoice not raised"
   - Right: "Generate Invoice" button (calls existing
     POST /api/milestones/{id}/invoice inline, removes row on success)
     and "View" button.

4. Update the panel header count to include the new types in the total.

5. Update the sort order rendering to match the backend sort:
   MISSED_DELIVERY → OVERDUE_PAYMENT → PAYMENT_DUE_SOON →
   UNINVOICED_TRIGGERED → UPCOMING_DEADLINE → UNINVOICED_MILESTONE.
   The frontend must not re-sort — render in the order the backend returns.

6. No new global state. Extend the existing local state in the dashboard
   component to handle loading/error states for the new inline action
   buttons ("Mark as Triggered" on MISSED_DELIVERY rows,
   "Generate Invoice" on UNINVOICED_TRIGGERED rows) using the same
   per-row loading state pattern already established.

no need to run any tests


log updates CLAUDE.md and Plan.md with the exisiting format