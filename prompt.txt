Refer CLAUDE.md and Plan.md. 

Read every file in the codebase before making any change. Then make only the following changes — nothing else. Do not touch any business logic, API behavior, data models, or frontend functionality.
The problem: Vercel Hobby does not support hourly cron jobs. We are removing the cron approach entirely and going back to the original lazy on-demand pattern — but adapted to work on Vercel serverless where BackgroundTasks silently fail.
The solution: Call run_pending_checks() synchronously, inline, on relevant GET requests instead of as a BackgroundTask. No cron, no background tasks, no scheduler of any kind.
STEP 1 — Remove the cron endpoint
Find the endpoint POST /api/milestones/check-now-cron that was added to routers/milestones.py. Delete it entirely — the function and its decorator. Do not touch any other endpoint in that file.
STEP 2 — Update vercel.json in the backend root
Replace the entire file with this — removing the crons block entirely:
json{
  "version": 2,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
STEP 3 — Restore lazy checks on GET endpoints, synchronously
Find the GET endpoints where run_pending_checks was previously called as a BackgroundTask and was then removed. These are most likely GET /api/contracts and GET /api/milestones/{contract_id} in routers/contracts.py and routers/milestones.py.
Add run_pending_checks(freelancer_id) back into these endpoints but call it synchronously and inline — not as a BackgroundTask. Place it before the main query that fetches and returns data, so checks run and update statuses before the response is built. Wrap it in a try/except so if it fails for any reason it does not block the response:
pythontry:
    run_pending_checks(freelancer_id)
except Exception:
    pass
Make sure run_pending_checks is imported in every file where you add this. Do not add the BackgroundTasks parameter back to any function signature — call it directly, not as a background task.
STEP 4 — Remove any CRON_SECRET references
Search the entire codebase for CRON_SECRET. Remove every reference to it — any import, any os.environ.get("CRON_SECRET") call, any usage. Since the cron endpoint is gone, this env var is no longer needed anywhere in the code.
STEP 5 — Verify nothing broke
After all changes:

Run python -m py_compile api/index.py and every modified Python file. Report any syntax errors.
Run python -c "from api.index import handler; print('OK')" from the backend root. Report the result.
Confirm vercel.json contains no crons key anywhere.
Confirm run_pending_checks is called synchronously in the GET endpoints and is wrapped in try/except.
Confirm the POST /api/milestones/check-now manual fallback endpoint is still present and completely unchanged — this one must stay.
Confirm no other files were modified beyond the ones listed in these steps.

Report exactly what changed in each file.

log updates CLAUDE.md and Plan.md with the exisiting format